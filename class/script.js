/**
 * Maximum data structure are makes by classes.
 * We can make own data structure  by class.
 * Class name start from capitale character.
 * For executuion of class code first make object or instence.
 * Three types of method we can make inside the class.[contructior , static , general]
 * static method call by class name while general method are called by instance.
 * new keyword is used for calling the constructor.
*/

class User {
  
}
const instance = new User;

console.log(typeof instance);

