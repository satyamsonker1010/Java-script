//Object.keys
/**
 * Object.keys() method which is used return an array
 * of a given object's own property names (Keys)
 *
 */

//Object.values
/**
 * The Object.values() method's behavior is similar
 * to Object.entries() method but it returns an array
 *  of values instead [key,value] pairs.
 */

//Object.entries
/**
 * The Object.entries() method is used to return
 * an array of a given object own enumerable string-keyed
 *  property [key, value] pairs, in the same order as that
 * provided by a for...in loop.
 */

const object = {
  a: "Good morning",
  10: 100,
  "hello ji": "how r u ",
};

//const entryVal = Object.entries(object);
// console.log(entryVal);

// for(let data of entryVal){
//   console.log(data[0], "=>",data[1]);
// }

// for(let [keys , val] of entryVal){
//    console.log(keys,"=>" , val);
// }



// const valArr = Object.values(object);
// console.log(valArr);



// const dataKeys = Object.keys(object);
// console.log(dataKeys);
// let keysData = [];
// for(data in object){
//   keysData.push(data);
// }
// console.log(keysData);